[pytest]
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: requires Snowflake connection (deselect with '-m "not integration"')
    unit: runs locally without external deps
addopts = -v --tb=short
